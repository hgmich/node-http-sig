{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^7.0.2"
  },
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
